apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-jk8s-api-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-jk8s-api-docker
  template:
    metadata:
      labels:
        app: sample-jk8s-api-docker
    spec:
      containers:
        - name: sample-jk8s-api-docker
          image: sample-jk8s-api-docker
          imagePullPolicy: IfNotPresent   # Set the image pull policy
          ports:
            - containerPort: 80
