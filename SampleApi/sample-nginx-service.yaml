apiVersion: v1
kind: Service
metadata:
  name: sample-nginx-service
  annotations:
    sidecar.istio.io/inject: "false"
spec:
  selector:
    app: sample-nginx-api-app
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP